CREATE DATABASE cadasto;
USE cadastro;
CREATE TABLE pessoas(
nome `varchar`(30),
idade `tinyint´(3),
sexo `char`(1),
peso ´float`,
altura ´float`,
nacionalidade `varchar(20)
);
describe pessoas;

//Esse são os primeiros comandos de um banco de dados MySQL//

